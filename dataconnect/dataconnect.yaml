specVersion: "v1beta"
serviceId: "sentry"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "sentry-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
