import 'package:flutter/material.dart';

class OfficeAddressPage extends StatelessWidget {
  const OfficeAddressPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Office/Work Address'),
      ),
      body: const Center(
        child: Text('Office/Work Address Details Here'),
      ),
    );
  }
}
